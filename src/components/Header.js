import Search from "./Search"
import Form from './Form'

const Header = () => {
    return (
        <div>
            <p>Header Logo</p>
            <Search />
            <Form />
        </div>
    )
}

export default Header